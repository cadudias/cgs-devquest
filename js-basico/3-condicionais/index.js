const boletoPago = true; /* boleto está pago */

/* estamos verificando se a condição é verdadeira */
if (boletoPago === true) {
  console.log('o boleto está pago');
} else {
  console.log('o boleto não está pago');
}
